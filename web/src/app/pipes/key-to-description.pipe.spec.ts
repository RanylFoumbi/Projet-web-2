import { KeyToDescriptionPipe } from './key-to-description.pipe';

describe('KeyToDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyToDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
